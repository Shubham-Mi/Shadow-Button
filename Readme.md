"Shadow Button" is a webpage with a `Click Me` button with a shadow which changes on user input.

- The button looks like coming out of the window on hover.
- the button looks getting inside the window on click.
